bash scripts that rip, tag, then flac.

Arch:
sudo pacman -S libdiscid perl perl-html-parser xmlstarlet flac cdparanoia

Ubuntu:
sudo apt-get install xmlstarlet perl flac cdparanoia

1. ./getbrainz.sh

2. cdparanoia -B

3. ./toflac.sh

4. ./move.sh
