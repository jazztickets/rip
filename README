bash scripts that rip, tag, then flac.

Arch:
sudo pacman -S libdiscid perl xmlstarlet flac cdparanoia

Ubuntu:
sudo apt-get install xmlstarlet perl flac cdparanoia

1. ./getbrainz.sh

2. rm *.wav && cdparanoia -B

3. ./toflac.sh

4. ./move.sh
